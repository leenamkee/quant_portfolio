# 작업 기록 (worklog)

최신 작업이 위에 옵니다. 작업할 때마다 이 파일에 기록하고 같은 커밋에 포함합니다 (규칙: [Agent.md](Agent.md) 참고).

## 2026-09-20

### 프로젝트 문서화
- `Agent.md`(에이전트/개발자 가이드)와 `README.md`를 새로 작성. 파일 역할, 데이터 흐름, 기본 종목·비중, DC 70% 룰, 수정 시 함정, 배포 제약을 정리.
- 이 문서에 "모든 작업을 worklog.md에 기록한다"는 규칙을 추가.

### 코드 점검에서 찾은 문제 수정
- **리밸런싱 날짜 누락 버그** (`rebalance_engine.py`): `data.resample(freq).last().index`는 달력상 기간 말일이라 휴장일과 어긋난다. 실제 데이터(2년, 5종목)로 확인한 결과 월간은 25회 중 15회, 분기는 9회 중 6회만 일치했고 **연간은 3회 중 0회라 리밸런싱이 한 번도 실행되지 않았다.** 각 기간의 마지막 실제 거래일을 쓰는 `get_rebalance_dates()`를 추가해 전부 일치하도록 수정.
- **비중 합이 1이 아닐 때 자산이 소멸**하던 함정 (`rebalance_engine.py`): 비중 합이 0.7이면 리밸런싱마다 자산이 70%로 줄어 수익률이 -99.97%가 나왔다. 백테스트 시작 시 비중을 정규화하도록 방어.
- **탭3 매수/매도 분류 버그** (`app_advanced.py`): 정수 컬럼을 문자열 `'0'`, `'-'`로 비교해 0주 종목도 매수 종목으로 집계됐다. 숫자 부호(>0, <0)로 분류하도록 수정.
- **중복 코드 제거** (`custom_backtest.py`): 백테스트/지표 함수가 `rebalance_engine`과 사실상 동일했다. 누락 티커 제거·정규화만 남기고 `backtest_rebalancing`, `calculate_metrics`를 가져다 쓰도록 변경. 위임 전후 결과가 동일함을 확인.
- **정의 중복 제거**: 종목명(`TICKER_NAMES`)과 목표 비중(`DEFAULT_TARGET_WEIGHTS`)을 `portfolio_engine.py` 한 곳으로 모으고, 앱의 기본 티커/탭2 비중/탭3 목표 비중 텍스트를 여기서 자동 생성. (기존에는 3곳에 따로 하드코딩)
- **통화 표기 통일**: 남아 있던 `$`를 원화로 변경 (탭2 초기 자본 기본값 10,000,000원, 탭3 요약 메트릭, `rebalancing_guide.py` 표 값, `app.py`/`app_advanced.py`의 남은 현금).

확인 방법: 전 파일 `py_compile`, 저장해 둔 실제 2년 가격 데이터로 old/new 리밸런싱 날짜 비교, `custom_backtest`↔`rebalance_engine` 결과 일치 확인, 월/분기/연/없음 백테스트가 정상 범위(약 +47~49%, MDD 약 -12.5%)인지 확인, 매수/매도 분류를 불균형 보유 예시로 확인.

남은 이슈
- `rebalancing_guide.get_current_prices`는 최신일이 아니라 직전 거래일 종가를 사용(의도 여부 미확인, 그대로 둠).
- 포트폴리오 저장/비교 기능은 아직 구현 전 (설계는 `docs/portfolio-storage-research.md`).

### 포트폴리오 저장 방법 리서치
- Streamlit Community Cloud에서 DB 없이 여러 포트폴리오를 저장·비교하는 방법을 조사해 `docs/portfolio-storage-research.md`로 저장 (claude.ai 문서에서 옮김).
- 결론: 로컬 디스크는 재부팅/재배포 시 사라지므로, GitHub API로 `data/portfolios.json`을 커밋하는 방식을 1순위, 브라우저 다운로드/업로드를 백업으로 추천. 사용자 확인 대기 중(PAT 사용 vs Google Sheets).

## 2026-09-19

### DC 퇴직연금 위험자산 70% 규정 반영 (`e1048fe`)
- 5개 ETF가 모두 위험자산(주식 40% 초과 혼합형, 실물 금, 주식형)으로 분류되어 위험자산 비중이 사실상 100%였음을 확인.
- 안전자산으로 KODEX 종합채권(AA-이상)액티브(273130.KS) 추가, 비중을 안전자산 30% + 위험자산 70%로 재배분(S&P500 25 / 채권혼합50 13 / 배당커버드콜 13 / 배당다우존스 12 / 금 7).
- 5종목 2년 데이터 분석: S&P500↔배당커버드콜 상관 0.91, 금·채권혼합50이 실질 분산 자산.

## 2026-09-18

### 국내 ETF 기본 포트폴리오 전환
- 기본 종목을 미국 개별주에서 국내 ETF 5종(KODEX 200미국채혼합50, TIGER 미국S&P500, ACE KRX 금현물, KODEX 미국배당커버드콜액티브, TIGER 미국배당다우존스)으로 변경. yfinance용 `.KS` 접미사 사용 (`79dbffe`).
- 초기 자본 단위를 원화로 변경, `target_weight` 최적화 방법 추가 (`79dbffe`).
- 리밸런싱 가이드: 기본 보유 수량(실제 보유 수량 반영)·목표 비중·종목명 컬럼 (`9823b1c`).
- 사용자 정의 백테스트 탭 기본값과 종목명 표시를 동일 포트폴리오로 정렬 (`5a70e1e`).
