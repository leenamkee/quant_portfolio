[pytest]
testpaths = tests
pythonpath = .
# 기대 실패(xfail)가 실제로 고쳐지면(통과하면) 실패로 처리해, 수정 시 표시를 제거하도록 강제한다
xfail_strict = true
addopts = -ra
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
    ignore::RuntimeWarning
